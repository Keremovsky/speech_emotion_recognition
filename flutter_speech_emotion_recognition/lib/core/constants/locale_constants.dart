import 'dart:ui';

class LocaleConstants {
  static const Locale en = Locale('en', 'US');
  static const Locale tr = Locale("tr", "TR");
  static const supportedLocales = [en, tr];
  static const path = "assets/lang";
}